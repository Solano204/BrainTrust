name: Backend CI/CD - Develop

on:
  push:
    branches:
      - develop
    paths:
      - 'backend/**'
      - '.github/workflows/backend-*.yml'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/backend

jobs:
  build-test-and-push:
    name: Build & Push Backend Docker
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    defaults:
      run:
        working-directory: backend

    steps:
      # ============================================
      # CHECKOUT CODE
      # ============================================
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      # ============================================
      # SETUP JAVA 25
      # ============================================
      - name: ☕ Setup Java 25
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'maven'
          cache-dependency-path: 'backend/pom.xml'

      # ============================================
      # BUILD ALL MODULES (SKIP TESTS)
      # ============================================
      - name: 🔨 Build All Modules
        run: |
          ./mvnw clean install -B -DskipTests=true
          echo "✅ All modules built successfully"

      # ============================================
      # PACKAGE APPLICATION
      # ============================================
      - name: 📦 Build Application Package
        run: ./mvnw package -DskipTests -B

      # ============================================
      # VERIFY JAR EXISTS
      # ============================================
      - name: 🔍 Verify JAR File
        run: |
          echo "Checking generated JAR files:"
          find . -name "*.jar" -type f | head -10
          ls -lh container-app/target/container-app-*.jar || echo "❌ JAR not found!"
          
          # Verify the specific JAR we need
          if [ -f "container-app/target/container-app-0.0.1-SNAPSHOT.jar" ]; then
            echo "✅ Found container-app JAR"
            ls -lh container-app/target/container-app-0.0.1-SNAPSHOT.jar
          else
            echo "❌ container-app JAR not found!"
            exit 1
          fi

      # ============================================
      # SETUP DOCKER BUILDX
      # ============================================
      - name: 🐳 Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ============================================
      # LOGIN TO GITHUB CONTAINER REGISTRY
      # ============================================
      - name: 🔐 Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ============================================
      # EXTRACT METADATA FOR DOCKER
      # ============================================
      - name: 🏷️ Extract Docker Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=develop-latest
            type=sha,prefix=develop-,format=short

      # ============================================
      # BUILD AND PUSH DOCKER IMAGE
      # ============================================
      - name: 🚀 Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend  # ✅ CRITICAL FIX: Point to backend directory
          file: ./backend/Dockerfile  # ✅ CRITICAL FIX: Full path to Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64
          build-args: |
            BUILDKIT_CONTEXT_KEEP_GIT_DIR=1

      # ============================================
      # SUMMARY
      # ============================================
      - name: ✅ Deployment Summary
        run: |
          echo "## 🚀 Backend Develop - Build & Push Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Docker Images Published:" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ☕ Java Version: 25" >> $GITHUB_STEP_SUMMARY
          echo "### 🐳 Base Image: eclipse-temurin:25-jre" >> $GITHUB_STEP_SUMMARY
          echo "### 📍 Build Context: ./backend" >> $GITHUB_STEP_SUMMARY